# Tools

This folder contains helper tools that sit outside the Ionic app.

Current tools:

- `resolve/` - DaVinci Resolve helper scripts for importing slate metadata into editorial workflows.

The Resolve helper is currently implemented as Lua so it can run from Resolve's
Workspace > Scripts menu without a separate Python install.
